---
- name: "Install basic packages (Ubuntu)"
  apt:
    name:
      - unzip
      - tar
      - curl
      - btrfs-tools
      - libseccomp2
      - socat
      - util-linux
      - apt-transport-https
      - ca-certificates
      - linux-headers-{{ ansible_kernel }}
    state: latest
